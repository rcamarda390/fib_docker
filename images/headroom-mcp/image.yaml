name: headroom-mcp
upstream_version: 0.36.3
# Published tags so far for 0.36.3: v1 (base image, superseded) and v2 (the
# same base with the Bedrock carry applied on top, when the carry still lived
# in its own Dockerfile). The carry now builds into the image directly, so v3
# onwards is a single self-contained build and v1/v2 are history.
revision: 2
build_args:
  HEADROOM_COMMIT: 87e71dd10057ff3cbe826bde617682971339e4f8
publish:
  ghcr: true
  dockerhub: true
  latest: false
